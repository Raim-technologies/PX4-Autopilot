#!/bin/sh
#
# Multicopter default parameters.
#
# NOTE: Script variables are declared/initialized/unset in the rcS script.
#

set VEHICLE_TYPE mc

# MAV_TYPE_QUADROTOR 2
param set-default MAV_TYPE 2

if param compare IMU_GYRO_RATEMAX 400
then
	param set-default IMU_GYRO_RATEMAX 800
fi

param set-default NAV_ACC_RAD 2

# vla_mock デモ規模 (離陸 5m / 巡航 3m) に合わせて RTL 高度を引き下げる。
# 既定の 30m/10m だと return で機体が一気に 30m まで急上昇してしまうため。
param set-default RTL_RETURN_ALT 6
param set-default RTL_DESCEND_ALT 3

param set-default GPS_UBX_DYNMODEL 6
